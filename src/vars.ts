const fetchEvents = {
  quryCrsr: { id: null, snapshot: null },
};

const listenSync = {
  removeListener: null,
};

const refreshEnroll = {
  timeId: null,
  seq: 0,
};

const vars = { fetchEvents, listenSync, refreshEnroll };

export default vars;
