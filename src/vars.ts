const me = {
  didFetch: null,
};

const profile = {
  stxAddr: null,
};

const profileEditor = {
  didFthAvlbUsns: false,
  didFthAvlbAvts: false,
};

const events = {
  didFetch: null,
};

const vars = { me, profile, profileEditor, events };

export default vars;
