const fetchEvents = {
  quryCrsr: { id: null, snapshot: null },
};

const listenSync = {
  removeListener: null,
};

const refreshEnroll = {
  timeId: null,
  seq: 0,
};

const refreshTxs = {
  timeId: null,
  seq: 0,
};

const vars = { fetchEvents, listenSync, refreshEnroll, refreshTxs };

export default vars;
